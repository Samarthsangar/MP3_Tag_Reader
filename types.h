//Haeder file for typdef & enum 
#ifndef TYPES_H
#define TYPES_H

//typedef of enum type for return value
typedef enum
{
    e_sucess,
    e_failure
}Status;

//typedef of enum type for return value
typedef enum
{
    e_view,
    e_edit,
    e_unsupported
}operation;

#endif